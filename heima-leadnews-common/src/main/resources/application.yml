#全局端口定义server:
# 全局字符集设置
spring:
  http:
    encoding:
      force: true
      charset: utf-8
      enabled: true
  aop:
    proxy-target-class: true
server:
  tomcat:
    uri-encoding: utf-8
mybatis:
  datacenter-id: 1
  workerId: 1
elasticsearch:
  host: 192.168.31.132
  port: 9200
